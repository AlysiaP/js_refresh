// Takes two string parameters and returns into following format: 'rank lastName reporting for duty!'
reportingForDuty = (rank,lastName) => {
  return `${rank} ${lastName} reporting for duty!`;
}

console.log(reportingForDuty('Private', 'Fido')) 
